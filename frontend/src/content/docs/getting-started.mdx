---
title: Getting Started
description: Get up and running with ShockStack quickly.
order: 1
---

import TerminalTabs from "../../components/content/TerminalTabs.astro";

# Getting Started

## Prerequisites

- Node.js (current LTS recommended)
- pnpm
- .NET SDK (needed if you want the optional backend/app host)
- Docker (needed for local containerized dependencies)

## Quick Start

<TerminalTabs
  title="Quick Start"
  variants={{
    linux: {
      lines: [
        {
          kind: "command",
          text: "git clone https://github.com/codywilliamson/shockstack.git my-project",
        },
        { kind: "command", text: "cd my-project" },
        { kind: "command", text: "pnpm install" },
        { kind: "command", text: "pnpm tokens:build" },
        {
          kind: "command",
          text: "dotnet run --project backend/src/ShockStack.AppHost",
        },
      ],
    },
    mac: {
      lines: [
        {
          kind: "command",
          text: "git clone https://github.com/codywilliamson/shockstack.git my-project",
        },
        { kind: "command", text: "cd my-project" },
        { kind: "command", text: "pnpm install" },
        { kind: "command", text: "pnpm tokens:build" },
        {
          kind: "command",
          text: "dotnet run --project backend/src/ShockStack.AppHost",
        },
      ],
    },
    windows: {
      lines: [
        {
          kind: "command",
          text: "git clone https://github.com/codywilliamson/shockstack.git my-project",
        },
        { kind: "command", text: "cd my-project" },
        { kind: "command", text: "pnpm install" },
        { kind: "command", text: "pnpm tokens:build" },
        {
          kind: "command",
          text: "dotnet run --project backend\\src\\ShockStack.AppHost",
        },
      ],
    },
  }}
/>

This starts a full local stack with frontend, backend, and supporting services.

## Frontend-Only Mode

<TerminalTabs
  title="Frontend Only"
  variants={{
    linux: {
      lines: [
        { kind: "command", text: "pnpm install" },
        { kind: "command", text: "pnpm tokens:build" },
        { kind: "command", text: "pnpm --filter frontend dev" },
      ],
    },
    mac: {
      lines: [
        { kind: "command", text: "pnpm install" },
        { kind: "command", text: "pnpm tokens:build" },
        { kind: "command", text: "pnpm --filter frontend dev" },
      ],
    },
    windows: {
      lines: [
        { kind: "command", text: "pnpm install" },
        { kind: "command", text: "pnpm tokens:build" },
        { kind: "command", text: "pnpm --filter frontend dev" },
      ],
    },
  }}
/>

Use this mode when you only need UI/content development.

## First Validation Pass

After startup, confirm:

- the frontend loads
- authentication pages render
- docs/blog routes render
- health endpoints respond when backend is enabled

## Next Steps

- [Project Structure](/docs/project-structure)
- [Design Tokens](/docs/design-tokens)
- [Frontend Guide](/docs/frontend-guide)
- [Authentication](/docs/authentication)
